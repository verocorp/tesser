# tesser

tesser is a Claude Code skill that answers a developer's question about an unfamiliar OSS dependency by cloning, building, and running it at a pinned SHA, with every load-bearing claim cited to file:line.

## Build contract — read before any tesser build session

The canonical build-contract artifacts (design doc, test plan, task list) and strategy context live on the maintainer's machine, not in this repo. Maintainers: the routing to those artifacts is in `CLAUDE.local.md` (gitignored, auto-loaded). Build sessions must read the contract artifacts before changing anything; this file carries implementation state and conventions only.

## Build status (update this ledger as part of each phase's final commit)

| Task | What | Status |
|------|------|--------|
| T1 | Repo scaffold | done, pushed (53682ed) |
| T2 | Specs: contract.yaml + digest-schema.yaml | done, pushed (0fa8bc8) |
| T10 | Spec repair: 2026-06-10 eng-review decisions D21–D30 (log-schema.yaml, grammar v1.1, pin-first, kind slugs, clause 8) | done (4d5cd07) |
| T11 | Spike: background-build→upgrade-in-place flow (D19 mechanics) in a throwaway skill | done (95277d1); clause 7 corrected — "upgrade in place" = superseding follow-up + ~/.tesser/ recovery; report in verocorp-tesser slug |
| T3 | scripts/log-invocation + logger tests | done (32bfd90; hardened 29b66f2) |
| T8 | scripts/validate-digest | done (594602a; hardened 29b66f2) |
| T4 | SKILL.md playbook | done (e9c48d4; review fixes ec4fb57); scorer-driven rework (Output contract: answer-first, silent steps 1–5, plain-language no-jargon, citation placement) traced to the T7a scoreboard failures on pyyaml-tesser — awaits Phase-3 maintainer taste gate + a fresh dogfood session scored to close the loop |
| T5 | 19 deterministic gate tests | done (54b415a; +6 review gates 29b66f2; +30 T12 gates 322c33f; +27 T4/review gates → 109 active) — B-gates armed |
| T7a | Scoreboard scorer — deterministic core (`tests/scoreboard/`: parse + score + cli + 19 tests) | done — reads scoreboard.yaml as data; FASTER/DIGESTIBLE/CALIBRATED-A computed; reproduces scoreboard-results.md numbers (GOLD tests against the real A/B sessions); `npm run score`. Deferred per spec: CALIBRATED Obligation B (agent-judged, RUN_EVAL); first-answer MARKER + `forbidden_lexicon_final` are surfaced maintainer decisions |
| T12 | Spec+code repair: 2026-06-11 close-review decisions D31–D38 + D22 resolution (host-aware digest layout, install-time deps, log/validator semantics) | done (322c33f) — T9 layout unblocked |
| T6 | E2E fixtures + suite | pending — to be redefined to pin against `scoreboard.yaml` (D39) after the post-dogfood revision pass |
| T7 | Eval stub (RUN_EVAL-gated) | in progress — deterministic scorer (T7a) + heuristic certification (recall+bias fixtures, ADVISORY) + **CALIBRATED-B LLM-judge WIRED** (`judge-rubric.ts` = the 8 dogfood principles + `KNOWN_FAILURE_MODES` self-corrections — "never trust yourself in THESE ways", targeted not global, per the YC paper-club method; `npm run judge -- <id>` spawns `claude -p`, grades the produced answer, prints the deterministic floor alongside). Validated: reproduces the gds/pi dogfood feedback AND caught the over-hedging the blind judge missed last round (the leniency failure-mode correction working). Remaining: win-condition matrix runner; adversarial-pass calibration; P3 SKILL.md rework iterated against the suite |
| T9 | Digest corpus seed | pending (supervised) |

### Shippability prep (2026-06-14, "make it shippable to a trusted alpha")
Cold-install path verified end to end: a `file://` clone delivers the right files (incl. `subagent-brief.md`), `scripts/setup` builds the venv from a fresh clone, and `scripts/log-invocation`/`validate-digest` run against the fresh venv with an isolated `TESSER_HOME` (closes the unverified-stranger-install risk the pending T6 would otherwise cover). README aligned to the current skill: the "What it does" section described the OLD always-build, grade-labeled, provisional-first model — rewritten to the two-beat, altitude-gated, no-leak, calibration-in-plain-English behavior; the "Bundled digests → starter set of libraries" claim was an overclaim (`digests/` is `.gitkeep`-only, T9 unseeded) and now states plainly that no digests ship pre-seeded. `CLAUDE.local.md` confirmed gitignored+untracked (no maintainer routing ships). **Two items remain, both Chris's call:** (1) the Phase-3 taste gate (read SKILL.md) — required by the checkpoint protocol before push; (2) the push itself — `origin/main` is at `34618ec` (Phase-2 close, a STUB skill), 27 commits behind, so a `git clone` today gets an unusable version; nothing is installable until the push. Noted-not-blocking: a clone ships the full dev repo (ledger, tests, eval specs) — inert but a minimal-distribution subset is a future cleanup.

Phase 1 (T1+T2) closed 2026-06-10: deterministic gates n/a (no tests exist yet), adversarial contract audit passed (a1083d2), code review run at high effort over the full phase diff (10 spec-level findings reported to the maintainer; spec files unchanged pending contract decisions), pushed.

Phase 2 plan eng-reviewed 2026-06-10: findings resolved into contract decisions D21–D30 (T10 carries the spec edits). **D22 (digest SHA-drift semantics on serve) is UNRESOLVED — must resolve before T4 (Phase 3); blocks the SHA-drift e2e only, not T3/T8/T5.** Deterministic gate count amended 13 → 19.

Phase 2 (T10+T11+T3+T8+T5) closed 2026-06-11: deterministic gates green (52 passed, 17 self-armed skips pending T4), adversarial contract audit PASS-WITH-FINDINGS (F1 README/clause-7 contradiction fixed a5f0bb7; F2 the 5-second self-update bound is an accepted authored constant under D23; F4 accepted then closed in 29b66f2), /gstack-review run cross-model over the phase diff (3 confirmed defects + mechanical hardening fixed in 29b66f2), pushed.

Close-review 2026-06-11 (/gstack-plan-eng-review over the parked findings, with Codex outside voice): all ten findings resolved into contract decisions **D31–D38** (host-aware digest layout amending D28's identity key to host + full namespace path; install-time deps via requirements.txt + scripts/setup amending the D1 install story; sha presence rule + logger gate; exit 4 environment failures; v1 trust posture + digest_sha256 logged passive-only; first-schema-valid-finalized-wins join rule; charset + repo-relative path tightening; --clone identity binding), and **D22 RESOLVED: async drift** — serve immediately labeled drift-unchecked, background clone computes, superseding follow-up delivers (the T11-proven mechanic). T12 carries all spec/code edits; full record in the phase2-close-report (verocorp-tesser slug) GSTACK REVIEW REPORT + eng-reviews/tesser-phase2-close-decisions in the brain.

Phase 3 (T12+T4) built 2026-06-11: deterministic gates green (109 passed, 0 skips — the 17 self-armed Type-B gates armed when T4 removed the STUB marker); adversarial contract audit over the T12+T4 diff returned "could not refute completion" (two soft spots fixed: D34 timeout exit-4 gate + digest/hash logger coupling, 5026106); /gstack-review cross-model (testing + maintainability specialists, Claude adversarial, Codex adversarial, red-team) — confirmed defects fixed in ec4fb57 (clone-lacks-commit→exit 4, control-char crash, replace-ref hardening, alias-bomb cap, BOM, drift-clone pin divergence, cold-persist cache-hit metric corruption), two robustness items deferred to TODOS under v1's accepted untrusted-code posture. **Not yet pushed — awaits the Phase-3 maintainer taste gate (Chris reads SKILL.md) before push.** Items surfaced for a maintainer decision (not unilaterally changed): digest selection tie-break formalization (clause-bearing?), whether ≥1 citation is required for a valid digest, and asymmetric logger enforcement (install-failure/completed presence rules — logger-gated vs analysis-side).

Course-correction 2026-06-11→12 (/gstack-office-hours + /gstack-plan-eng-review): the evening red-team of the BARE agent (PyYAML/coral/caffeinate/kinesis) found tesser's experience is worse than the default regardless of question (warm 13.7× slower; on coral — the founding example — bare won 13/14 via WebFetch). Diagnosis (Chris): the 109 gates test OBEDIENCE to SKILL.md, not value. Outcome: **D39 — `scoreboard.yaml`, the outcome-anchored experience contract.** Three axes vs the default-agent null baseline, across the FULL dep range incl. simple deps: **FASTER** (time-to-first-*answer*, UX-TTFR model, hard gate <10s ideal/≤30s — first emission answers now + offers to deepen, not "I'm working on it"), the truth axis (see D40), **DIGESTIBLE** (structural-only s2n pins incl. no-internal-lexicon-leak; functional/completeness scoring rejected). Locked as draft; maintainer will dogfood then we revise (scheduled re-open). T6/T7 redefined to pin against it. The "stop testing setup questions / narrow to a wedge" direction from the evening session was REJECTED by Chris — simple deps must beat the default too.

2026-06-12 scoring + D40 reframe: scored the default column on 5 cells (`scoreboard-results.md`), incl. the first EXTERNAL untrusted clone (Chris McLean's codeharness, with/without A/B). Two opens settled from data (ttfa_baseline_rule asymmetric; noise→placement+length checks). **Codeharness finding:** tesser's run-grade upgrade was DENIED by the host auto-mode classifier ("External/Untrusted Code") — the answer was unchanged (already inspect-grade), so the run-grade differentiator was a *no-op* on its real target. **D40 — truth-axis reframe (Chris):** rename `truer` → **CALIBRATED** and scope v1 to JTBD **#2 (convey how true / calibration: Obligations A provenance + B faithful confidence + the covered/known-gap/unknown-gap map, never overclaim)**, DEFERRING JTBD **#1 (make/verify true — run-grade correctness truth-delta vs oracle) to v2** (where sandboxing/hosted tier unblocks the wall). Grounded in Chris's own locked `originals/trust-a-claim-as-much-as-it-deserves` (#1/#2 split) + `findings-are-max-entropy-distributions` (overstatement = sub-entropy claim). This dissolves the run-grade wall (#2 needs no execution) and aligns all three axes on the short, honest, gap-flagged first answer. A 3-bullet calibrated answer scores HIGHER than a 3k overclaiming one.

2026-06-13 dogfood feedback + instrument reframe (course-correction, /gstack-plan-eng-review): Chris dogfooded gds-agent + pi (with/without). Three reframes from his feedback: (1) **altitude is a spectrum** (quick-overview → full-build-and-test); build/verify must be altitude-gated, not always-on (the gds build self-declined for an overview question — §7 always-verify fought the altitude). (2) **The instrument is NOT the vs-default win-condition** — Chris doesn't trust/care about comparison; he wants his PRINCIPLES encoded as automated tests on tesser's OWN output, iterated against until happy, regression-protected. Quality gate = structural gates + Chris judges + an **LLM-judge bootstrapped from his accumulated judgments** until it replicates him (so capturing his per-session verdicts as a labeled corpus is first-class). (3) **UX**: the ideal in-place 3-line scroller is NOT possible in Claude Code (append-only chat, tool-blocks unhideable — confirmed via claude-code-guide); the achievable path is **subagent-isolating the overview machinery** (clone/build/survey run in a fork the user never sees; map → ~/.tesser; main thread shows plain progress one-liners + short summary + JTBD close). Conflicts with §6 background-build (can't subagent the upgrade path) → altitude split. Plan: P1 trustworthy structural instrument → P2 iterate-harness + label-capture → P3 SKILL.md rework (subagent-isolate, altitude-gate, JTBD close) → P4 LLM-judge trained on the P2 corpus. **P1 done** (scorer trust-fixes), **CALIBRATED-B judge wired** (judge-rubric.ts + KNOWN_FAILURE_MODES, `npm run judge`), **P3 done** (SKILL.md reworked: altitude-gating — overview reads-and-summarizes, no build; §7 always-verify → verify-when-the-altitude-calls; subagent-isolate the overview machinery with plain progress lines; killed the machinery tail + `~/.tesser` disk line; single JTBD close; like-X-but-Y + do/don't + no-overclaim — traced to the judge's gds verdict; 184 gates green, 15772/16384 bytes). Handed off to Chris for fresh manual runs scored with `npm run score` + `npm run judge` to validate skill+judge. Below — the P1 detail: scorer trust-fixes from his calibration (F1 TTFA finds real answer via non-narration filter, no spurious warning; F3 dropped "the skill" as over-broad — D41 amendment; F4 gap recall broadened to caveat/fork/"couldn't fully verify"; F5 advisory overclaim markers catching "instant"), real-session recall/bias fixtures, suite 180 green.

2026-06-13 (cont.) judge-fix + two-beat SKILL rework from a fresh dogfood (4 runs: mosh/tmux stumbled-into, gds warm-cache, gds cold+friends, + a no-skill baseline). Chris ran the judge on the 3 valid sessions, then gave feedback; two instrument bugs surfaced + got fixed, then the skill reworked to his "excellent turn" spec. **(1) Judge made turn-aware** (`answerTurns` in parse.ts): the old `extractAnswer` concatenated the whole session into one blob AND stripped the narration lead, so principle 1 was BLIND to the "I'll use the tesser skill" opener (scored PASS while the floor said ✗) and principle 2 couldn't tell single-answer length from cross-turn repetition. Now segments into (question→answer) turns, breaking only on REAL dev input (`isInjectedTurn` drops skill-load/`<task-notification>` machinery turns so a narration stub + its substance stay ONE answer); narration kept. **(2) Judge made machinery-aware** (Chris: "it's not a judge if it can't evaluate properly"): it graded PROSE ONLY, blind to the bash blocks the dev watched. Now parse builds a `displayEvents` stream (text + tool calls interleaved, `⟨tool $ …⟩` markers); **principle 9 "machinery silence"** added (8→9) — penalizes tool blocks bracketing the answer, progress narration, and a no-op success report ("drift check: no drift — everything is current"); grounding stays silent unless it CHANGES the answer. Proven: re-judging gds-cache flipped answer-first PASS→FAIL and named machinery-silence the weakest principle, articulating the two-beat fix itself. **SKILL rework (P3-v2)**: the overview is now **two beats** — kick off the grounding in a background sleuth subagent + answer immediately from KNOWLEDGE (a citeable input under CALIBRATED; <30s, no foreground bash), then the sleuth confirms SILENTLY or emits a superseding correction. Killed "drift check"/"drift-unchecked"/"no drift" success narration; citations live in the map not a chat provenance block. **D42**: "drift check" added to `forbidden_lexicon` (meaningless to the dev). **gate 7** (6 new structural gates) locks the two-beat shape; gate 6b auto-extended. 191 gates green, 16379/16384 bytes. Handed back to Chris for another dogfood run. Deferred: judge-parse unit tests (`answerTurns`/`displayEvents`/counts have no coverage — silent-failure-prone; offered, redirected to the skill first).

2026-06-13 (cont.) first REAL headless run of the reworked skill, scored+judged, then fixed. Ran `claude -p "tell me about github.com/sourcegraph/conc"` from `tesserts/with` (plain prompt; `--dangerously-skip-permissions` was DENIED by the auto-mode classifier, plain `claude -p` allowed — tools still ran). Session `bebf09d9`. **The two-beat ARCHITECTURE engaged**: tool counts `Skill:1, Bash:1, Agent:1` with `run_in_background:1` — one backgrounded sleuth, one foreground Bash, zero `⟦⟧@sha` markup / provenance block / drift-check; content strong (judge PASS on no-overclaim, do/don't, mental-model, JTBD close). **But execution missed the timing+silence beats** (floor: answer-leads ✗, TTFA 50.6s; judge weakest = machinery-silence FAIL, answer-first FAIL). Timeline diagnosis: 6.6s "I'll use the tesser skill" narration → 17s FOREGROUND bash (self-update+log+map-check) → 33.7s sleuth spawned → 50.7s answer. Root cause: §1–§9 read as an imperative foreground sequence that fought the two-beat's "the sleuth runs steps 1–6"; the agent ran steps 1–3 foreground. Plus it pre-announced the grounding ("I'm grounding all of the above in the background… I'll follow up to correct it") and LEAKED "sleuth"/"map". **Fixes (this commit)**: (1) two-beat reframed — "**In the main conversation you do exactly two things**; everything in §1–§9 is the background subagent's job, never a foreground step"; first action = spawn subagent + answer, do NOT run git pull/log/map-check foreground. (2) beat 2 forbids pre-announcing a running check (the coda is the exact noise to cut); "the developer never hears the machinery's words". (3) **D43**: "sleuth" added to `forbidden_lexicon`; SKILL ban lists it + body renamed "the sleuth"→"the (background) subagent" to kill the leak temptation. §1/§2/§4 compressed to hold the 16384 budget (16373). 114 change-relevant gates green (logger.test.ts flaked on a load-69 machine — subprocess timeouts, not a regression; scripts untouched). Re-ran to validate.

2026-06-14 two more dogfood runs scored + diagnosed (/gstack-plan-eng-review), then instrument-fix + playbook-split (D44, D45). Chris ran two more `/tesser` sessions from `tesserts/with`: `617e9e78` (`codeharness vs pi` — a comparison, one repo private) and `d5eec575` (`tell me about neo4j graph data science` — overview, then escalated to building a custom Pregel algo). **Headline: the scorer misgraded its BEST run as its worst.** Session 2's two-beat ENGAGED (background Agent spawned 21s, clean leading GDS answer 36s, then altitude escalated cleanly overview→build, hit a Java 11-vs-17/21 wall, found JDK 23, compiled+tested+produced the plugin JAR, flagging the gap honestly). But the scorer reported it as **267.5s TTFA / answer-doesn't-lead** — FALSE. Root cause: the NARRATION lexicon's bare `/\bclassification\b/` fired on the answer's legitimate "node classification and link prediction", discarding the real 36s answer as process-narration (the 2nd over-broad-term collision after "the skill", D41). Session 1 scored accurately: two real SKILL defects — the two-beat **never engaged** (no subagent, foreground clones, 59s) and an **overclaim** ("codeharness doesn't exist" when it is private+accessible; the 404 is ambiguous; Chris corrected it). SKILL.md was at 16373/16384 — 11 bytes of headroom — so any skill fix had to displace content. Diagnosis traced to the spawn prompt: the main thread re-typed §1–§9 into the subagent prompt by hand, so the machinery was paid for 3× (loaded, re-derived into the spawn prompt, re-read by the subagent) — the source of the byte ceiling, the slow pre-spawn TTFA, and the foreground-conflation bug. **D44 (instrument):** the narration lexicon may not list a bare domain word a real answer would use — anchor collision-prone terms to tesser-machinery context (`classification`→kind-slug proximity, `pinned at`→following sha, `self-update`→report form) and drop `cold path` (redundant with cold run/no digest, collides with the hot/cold-path perf idiom); a collision-corpus regression test (`score.test.ts`) pins both directions (GDS/conc/pi real answers stay quiet; pure machinery leaks still flag). Re-scored session 2 → true **36.3s / answer-leads ✓**. **D45 (playbook split):** SKILL.md is now a thin altitude router (override rules + dev-facing output contract + the foreground build path); the grounding machinery moved to **`subagent-brief.md`** (own 12288-byte budget) read by the background subagent for an overview or inline by the main thread for a build. Structure gates 1/2/5 read the union of both files; gates 4/6/7 stay SKILL.md-only; gate 4 gained a brief byte-budget check (contract.yaml `subagent_brief.max_bytes`). Added the **overclaim rule** — an inaccessible clone (404/auth) is "can't reach", never "doesn't exist". SKILL.md **16373→9556 bytes** (6.8KB headroom); subagent-brief.md 9037 bytes. Full suite **202 passed / 4 skipped** (the 4 are the pending-T6 e2e). Session 1's 59s and the comparison two-beat miss are addressed structurally (one-line spawn prompt, no re-derivation, no foreground checklist to run by mistake) but await a fresh dogfood to re-validate. **Re-validated same session (2026-06-14) with two fresh `claude -p` runs from `tesserts/with`: overview `5814638c` (`/tesser p-limit`) and comparison `6e0aea27` (`/tesser zerolog vs zap`). BOTH passed FASTER for the first time — overview TTFA 25.9s ✓, comparison TTFA 29.0s ✓, both answer-leads ✓, both lexicon-leak 0, both two-beat engaged (Skill + one backgrounded grounding subagent spawned FIRST: p-limit 13.6s→answer 25.9s; zerolog/zap 11.7s→answer 29.0s, both repos cloned by the subagent not the main thread). The session-1 two-beat-on-comparison defect is FIXED; beat 2 stayed silent (no noisy follow-up — the knowledge answer was right). Caveat: a PLAIN-prompt comparison (no `/tesser`) did NOT auto-invoke the skill (`tools: {}`) and the bare model leaked "tesser workspace" — a skill auto-invocation reliability gap with plain prompts, not a content issue; `/tesser` engaged cleanly every time. Note `claude -p` is single-turn so beat 2's superseding-correction path is only exercisable in an interactive session.** Handed back to Chris for the next runs.

2026-06-14 (cont.) Letta eval + beat-2 supersede fix (D46). Three more runs scored/judged: `/tesser Letta` (d8ba90e1, interactive multi-turn), the default-agent Letta baseline (79ab5b06), and a crafted beat-2 forcing test (3e0740d7, Chris fed a Letta description and asked the agent to spawn the subagent on it). **Beat 2 confirmed working in a REAL interactive session** (the path single-turn `-p` couldn't reach): d8ba90e1 spawned bg subagent at 10.9s, led the answer at 24.4s (TTFA ✓), and at 215.7s emitted a substantive source-grounded supersede (repo is Letta's backend not the whole product; a 4th memory tier the knowledge answer missed; the memgpt-heartbeat loop is the original design with newer agent types moving away). Head-to-head **tesser WINS** vs default (faster 24.4 vs 28.6s, tighter, plus a verified correction the default can't give; CALIBRATED ties on the deterministic floor — which UNDERSELLS tesser because beat-2 quality is the agent-judged Obligation-B tail). `npm run judge` on d8ba90e1: 6 PASS / 3 PARTIAL, weakest = **principle 9 machinery-silence**, one precise seam — the supersede narrated the ACT of grounding ("now that I've checked it against the actual source") instead of just stating the corrected fact, dragging principle 1 down with it. **D46:** beat 2 now leads with the corrected fact and **never narrates the act of grounding** ("now that I've checked the source" / "having read the code" / "the grounding found") — machinery-silence extended from beat 1 to beat 2; a grade change like "confirmed by running it" stays in the build path (§8) because that is calibration the dev wants, not machinery. New gate-7 structural pin locks it; 53 structure gates green, SKILL.md 9939 bytes. **Two future items tracked in TODOS.md (Chris, deferred):** (1) first-answer readability/scannability — the beat-1 answer is still a wall of text; wants structure for a 3-second skim (invisible to the scorer today). (2) voice/tone — reframe the supersede as progressive enhancement ("quick answer" → "better info arrived"), not self-correction/"I was wrong"; register work, best as a judge principle + writing-style note, pairs with the scoreboard `forbidden_lexicon_final` open. Still unpushed (Phase-3 taste gate).

2026-06-12 scorer + SKILL.md rework + D41: built **T7a** — the durable scoreboard scorer (`tests/scoreboard/`, `npm run score`), spec-as-data against `scoreboard.yaml`, reproducing the `scoreboard-results.md` numbers as GOLD tests; it's the first instrument testing VALUE not OBEDIENCE. Used its verdict to drive a scorer-traced **SKILL.md rework** (Output contract: answer-first, silent steps 1–5, plain-language no-jargon, citation placement; keep systematic provenance — the one auto-mode-surviving edge). Applied the `concepts/testing-agent-skills.md` pyramid to the leak axis: the exact-zero pin belongs in the FREE Type-B ring, not an expensive model run — added gate 6a (no bracketed grade-label template can regress) + gate 6b (SKILL.md's Output-contract ban must document the full lexicon, spec-as-data sync). **D41 — finalized `forbidden_lexicon`** (closing the last scoreboard open): the full grade ladder (run/inspect/docs/truth-grade) + provisional + cold run + digest + citable knowledge + kind slug + "the skill" + raw citation markup; renamed `forbidden_lexicon_seed`→`forbidden_lexicon`. Frozen-session leak recount 24→27 (the seed omitted inspect/docs-grade). Suite 141 green. Only the maintainer dogfood-then-revise lever remains open on the scoreboard. Still unpushed — Phase-3 taste gate.

## Decision namespaces

Bare `Dn` in spec files and build artifacts = the canonical design/test-plan decision list (D1–D20 in the design-doc addendum; new contract decisions continue at D21+). Repo-process decisions use a `Pn` prefix going forward. Commit messages before 2026-06-10 ("D1 = full ladder", "D2=A", "D3" routing split) predate this convention and refer to the process list, not the canonical one.

## Orchestration rules

- One subagent per T-task, EXCEPT: T2 specs and T4 SKILL.md are authored in the main loop (taste-bearing), and T9 pre-runs are always supervised.
- Commit per task, files staged individually; the orchestrating session commits, not subagents.

## Phase checkpoint protocol (decided 2026-06-10, D1 = full ladder)

A phase is done only when every applicable rung passes and the build-status ledger above is updated in the phase's final commit:

1. **Deterministic gates** — every test that exists as of the phase passes.
2. **Adversarial contract audit** — a fresh-context subagent (never the builder) receives the phase's tasks-JSONL lines, the relevant D-decisions, and the phase diff, and attempts to REFUTE "this phase is complete as specified." It returns a per-criterion met / unmet / uncheckable table with file:line citations, plus any silent additions beyond spec. Unmet findings are fixed or explicitly accepted before the phase closes.
3. **Code review** — /gstack-review over the accumulated phase diff before push.
4. **Human taste gate** — Phase 3: maintainer read of SKILL.md; Phase 5: maintainer approves every digest (rubric gold). Other phases: none.

## Repo rules

- All persisted runtime state lives under `~/.tesser/` — never inside this repo.
- `digest-schema.yaml`, `contract.yaml`, and `scoreboard.yaml` are spec-as-data: tests pin against them. Change them only with a matching contract decision.
- `SKILL.md` and the spec files are taste-bearing files owned by the main build loop, not subagents.
